#include <stdio.h>

int main() {
    int rows = 4; // Number of rows
    int cols = 8; // Number of columns
    int i, j;

    for (i = 0; i < rows; i++) {
        for (j = 0; j < cols; j++) {
            printf("$");
        }
        printf("\n");
    }

    return 0;
}
